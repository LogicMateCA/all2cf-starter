import { useEffect,useState,type FormEvent } from "react";import { authClient } from "@/lib/auth-client";import { Button } from "@/components/ui/button";import { Input } from "@/components/ui/input";import "./phone-page.css";export function PhoneAuthPage(){const [signedIn,setSignedIn]=useState(false);const [phone,setPhone]=useState("");const [password,setPassword]=useState("");const [code,setCode]=useState("");const [sent,setSent]=useState(false);const [busy,setBusy]=useState(false);const [error,setError]=useState("");useEffect(()=>{void fetch("/api/session").then((response)=>setSignedIn(response.ok)).catch(()=>setSignedIn(false));},[]);async function signIn(event:FormEvent){event.preventDefault();setBusy(true);setError("");const result=await authClient.signIn.phoneNumber({phoneNumber:phone,password,rememberMe:true});setBusy(false);if(result.error)setError("Phone number or password is incorrect.");else window.location.assign("/app");}async function send(){setBusy(true);setError("");const result=await authClient.phoneNumber.sendOtp({phoneNumber:phone});setBusy(false);if(result.error)setError("Unable to send a verification code.");else setSent(true);}async function verify(){setBusy(true);setError("");const result=await authClient.phoneNumber.verify({phoneNumber:phone,code,updatePhoneNumber:true,disableSession:false});setBusy(false);if(result.error)setError("The code is invalid or expired.");else window.location.assign("/app/settings");}return <main className="phone-auth"><section><span>Phone authentication</span><h1>{signedIn?"Add or change your phone":"Sign in by phone"}</h1>{signedIn?<div className="phone-flow"><p>A six-digit code is delivered by the selected Twilio provider. Numbers must use E.164 format.</p><Input type="tel" autoComplete="tel" value={phone} onChange={(event)=>setPhone(event.target.value)} placeholder="+14035551234"/>{sent?<><Input inputMode="numeric" autoComplete="one-time-code" value={code} onChange={(event)=>setCode(event.target.value)} maxLength={6} placeholder="123456"/><Button disabled={busy||code.length!==6} onClick={()=>void verify()}>Verify phone number</Button></>:<Button disabled={busy||!phone} onClick={()=>void send()}>Send verification code</Button>}</div>:<form onSubmit={(event)=>void signIn(event)}><Input type="tel" autoComplete="tel" value={phone} onChange={(event)=>setPhone(event.target.value)} placeholder="+14035551234"/><Input type="password" autoComplete="current-password" value={password} onChange={(event)=>setPassword(event.target.value)} placeholder="Password"/><Button type="submit" disabled={busy||!phone||!password}>Sign in</Button></form>}{error?<p role="alert">{error}</p>:null}</section></main>;}
