import { useEffect,useState } from "react";import { createAuthClient } from "better-auth/client";import { oneTapClient } from "better-auth/client/plugins";import "./one-tap-page.css";export function GoogleOneTapPage(){const [error,setError]=useState("");useEffect(()=>{let active=true;void fetch("/api/google-one-tap/config").then(async(response)=>{if(!response.ok)throw new Error("Google One Tap is not configured.");return response.json() as Promise<{clientId:string}>;}).then(async({clientId})=>{if(!active)return;const client=createAuthClient({baseURL:window.location.origin,basePath:"/api/auth",plugins:[oneTapClient({clientId,autoSelect:false,promptOptions:{fedCM:true,maxAttempts:3}})]});await client.oneTap({callbackURL:"/app",button:{container:"#google-one-tap-button",config:{type:"standard",theme:"outline",size:"large",text:"continue_with",shape:"rectangular",width:320}}});}).catch((caught:Error)=>{if(active)setError(caught.message);});return()=>{active=false;};},[]);return <main className="one-tap-auth"><section><span>Google identity</span><h1>Continue with Google</h1><p>One Tap uses the same configured Google OAuth client. Auto-selection is disabled so account choice remains explicit.</p><div id="google-one-tap-button" aria-label="Google One Tap sign-in"/>{error?<p role="alert">{error}</p>:null}</section></main>;}
