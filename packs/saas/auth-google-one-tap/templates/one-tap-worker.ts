import { Hono } from "hono";import type { AuthRuntimeEnv } from "../auth-runtime";const feature=new Hono<{Bindings:AuthRuntimeEnv}>();feature.get("/api/google-one-tap/config",(c)=>{const selected=new Set(String(c.env.AUTH_SOCIAL_PROVIDERS||"").split(",").map((value)=>value.trim()));if(!selected.has("google")||!c.env.GOOGLE_CLIENT_ID)return c.json({enabled:false},503,{"Cache-Control":"no-store"});return c.json({enabled:true,clientId:c.env.GOOGLE_CLIENT_ID},200,{"Cache-Control":"no-store"});});export const oneTapFeature=feature;
