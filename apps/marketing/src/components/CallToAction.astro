---
interface Props { title: string; body: string; href?: string; label?: string; }
const { title, body, href = "/login", label = "Get started" } = Astro.props;
---
<section class="cta-section"><div><h2>{title}</h2><p>{body}</p></div><a class="primary-button" href={href}>{label}</a></section>
